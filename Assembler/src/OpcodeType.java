/**
 * @author Nathan Robinson
 */

public enum OpcodeType {
    HALT, PUSH, RVALUE, LVALUE, POP, STO, COPY, ADD, SUB, MPY, DIV, MOD, NEG, NOT,
    OR, AND, EQ, NE, GT, GE, LT, LE, LABEL, GOTO, GOFALSE, GOTRUE, PRINT, READ, GOSUB, RET
}
